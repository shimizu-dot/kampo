# コピーして .env を作成し、ローカルでの docker run / docker compose の確認に使う。
# Render では .env は読み込まれないため、本番の値はRenderダッシュボードの
# Environment Variables に設定する(render.yaml 経由の場合はDB系は自動設定される)。

PORT=8080
DB_HOST=localhost
DB_PORT=5432
DB_NAME=kanpo
DB_USER=postgres
DB_PASSWORD=hs0512
